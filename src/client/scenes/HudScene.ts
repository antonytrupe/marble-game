export class HudScene extends Phaser.Scene {

    constructor() {
        super({
            key: 'HudScene',

        })
    }

    preload() {
    }

    init(): void {
    }

    create() {
    }

    update(time: number, delta: number): void {
    }
}